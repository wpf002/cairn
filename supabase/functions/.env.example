# Server-side secrets for Edge Functions. Read by `supabase functions serve`
# locally; for the hosted project use `supabase secrets set KEY=value`.
# Nothing in this directory is ever bundled into the app.
ANTHROPIC_API_KEY=
