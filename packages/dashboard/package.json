{
  "name": "@cairn/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "View-model builders for TODAY, the per-child dashboard, and the 21-year roadmap",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "dependencies": {
    "@cairn/framework": "workspace:*",
    "@cairn/stages": "workspace:*",
    "@cairn/substrate": "workspace:*"
  }
}
