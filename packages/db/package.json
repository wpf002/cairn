{
  "name": "@cairn/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Data model: Person rows, time-bounded relationship edges, family records",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "dependencies": {
    "@cairn/framework": "workspace:*",
    "@cairn/stages": "workspace:*"
  }
}
